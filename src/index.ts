console.log('VALR Grid Trading Bot starting up...');

function greet(name: string): string {
  return `Hello, ${name}! Your bot is ready to roll.`;
}

const botName = 'valr-bot';
console.log(greet(botName));
